/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
};
module.exports = {
  i18n: {
    locales: ["fa"],
    defaultLocale: "fa",
  },
};

module.exports = nextConfig;
